{/*import Image from "next/image";
import { styles } from "../../../../../utils/styles";
import { useTranslations } from "next-intl";
import { getStellactrumColor } from "../data/stellactrum-utils";
import { SkillWeaponTableRowProps } from "@/app/utils/others";

export default function SkillWeaponTableRow ({
    icon,
    title,
    label,
    cooldown,
    cost,
    data

} : SkillWeaponTableRowProps) {

    const t = useTranslations('');
    const displayStellactrum = getStellactrumColor(stellactrum);


    return (
        <div>

        </div>
    )
}
*/}